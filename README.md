# Proyecto Final - Pruebas de API

Este proyecto contiene pruebas automatizadas de API utilizando **Python**, **pytest** y **requests**.  
El objetivo es validar endpoints públicos cubriendo diferentes métodos HTTP y generar reportes HTML.

## 🚀 Instalación

```bash
pip install -r requirements.txt
